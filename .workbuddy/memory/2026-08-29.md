# 2026-08-29

## M5③ 消息幂等加固 — 崩溃重试测试放弃 & 数据恢复
- 用户自行删除了 `StockDeductConsumer` 中的临时代码（崩溃测试 `if(locked) throw` / `forceCrash` 逻辑），决定不测试崩溃重试。
- 已把测试产生的脏数据恢复至测试前状态：
  - 删除 `order_info` id=20 及其 `order_item`（MySQL，库 ruyi_mart）
  - 商品 17 库存由 99 恢复为 100
  - 删除 Redis 幂等键 `ruyi:mq:stock-deduct:20`（db0）
- 现状：幂等状态机代码（KEYS 标签 + setIfAbsent PROCESSING/SUCCESS/FAILED）保留，仅移除崩溃测试桩。
- 注意：RocketMQ 存储仍在 E:\rocketmq_store；broker 重启后需重启 Spring 应用以重新建立消费者订阅（历史踩坑）。

## M5③ 收工：干净验收 + 提交
- 代码检测：OrderEventProducer（KEYS lambda 版）、StockDeductConsumer（纯状态机，崩溃桩已清）均正确。
- 后台 mvn 启动失败（本机 Maven classworlds 加载错误，环境问题非代码问题）；改用 IDEA 启动应用（8080 通）。
- 删桩后跑干净端到端验收：登录→加购商品17→下单 订单21 → 消费成功：Redis `ruyi:mq:stock-deduct:21`=SUCCESS、`order_info.status`=1、商品17库存 100→99。✅
- 收尾：清验收脏数据（订单21+库存回100+Redis键）、删 5 个排错临时文件（check_ports.txt/last_orderid.txt/rmq_check.txt/fix_bat.ps1/restart_rmq.ps1）。
- 提交：b6f5afb `feat(order): 消息幂等加固——生产者KEYS标签+消费者状态机防重复消费`（仅提交两个 mq 代码文件，未提交 .workbuddy/）。
- 待定：是否 push 到远程 ruyi_mart/master（目前本地领先，未 push）。

## 订单查询业务（已完成，待提交）
- 用户自行在 IDEA 敲完 3 个文件（未授权阿瑞代改）：`OrderService`（加 `listOrders`/`getOrderDetail` 声明）、`OrderServiceImpl`（实现 + 私有 `toVO`）、`OrderController`（加 `GET /order/list`、`GET /order/{id}`，抽 `currentUserId()`）。
- 阿瑞只用 curl 跑验证（用户一次性授权本次）：登录→加购商品17→下单 order22→`GET /order/list` 返回数组含明细✅、`GET /order/22` 返回单订单含 items✅。下单返回 status=0，查列表时消费者已异步置 status=1（符合预期）。
- 验证后恢复数据：删 order22 + 明细、商品17库存回100、删 Redis 键，核对四项全绿。
- **MySQL 带空格路径坑（重要）**：在 Git Bash 里用变量存 `/d/MySQL/MySQL\ Server\ 8.0/bin/mysql.exe`（`MYSQL="/d/MySQL/MySQL\\ Server\\ 8.0/bin/mysql.exe"`）会导致 Exit 127 命令找不到；**必须直接写字面路径** `/d/MySQL/MySQL\ Server\ 8.0/bin/mysql.exe -uroot -p123456 ...`（用 `\ ` 转义空格），不用变量。Redis 路径无空格不受影响。
- 下一步业务：超时关闭（用户已定方案A「锁库存+超时回补」+ 顺手 Mock 支付，但要求每次只给一个业务）。

## 超时关闭业务（代码已全部给完，用户敲完编译通过，待验证）
- 4 个文件（均为文本给法，用户自敲，未授权阿瑞代改）：
  1. 新建 `module/order/task/OrderTimeoutCloseTask.java`：用户用 `@Slf4j`（沿用苍穹外卖风格），`@Scheduled(fixedDelay=60000)` 每分钟扫 `status=0 且 create_time 超 TIMEOUT_MINUTES(默认30)` 的订单 → 查 Redis 幂等键 `ruyi:mq:stock-deduct:{id}`：SUCCESS 则回补库存(productService.addStock)再置 status=3(CLOSED)；否则直接关单不回补。`@Transactional` 保证一致。
  2. `RuyiMartApplication` 加 `@EnableScheduling`（启动定时调度，否则 @Scheduled 不生效）。
  3. 改 `StockDeductConsumer`：扣库存成功由置 `status=1` 改为保持 `status=0`(PENDING/待支付锁库存)，失败仍置 2(STOCK_FAILED)。**最小改动**：加 import OrderStatus、删 STATUS_CONFIRMED/STATUS_STOCK_FAIL 常量、第94行改 `allOk ? OrderStatus.PENDING.getCode() : OrderStatus.STOCK_FAILED.getCode()`。未动其 Logger 写法（尊重"老文件先不动"）。
  4. 新建 `module/order/enums/OrderStatus.java`：PENDING(0)/PAID(1)/STOCK_FAILED(2)/CLOSED(3)，替代魔法数字。
- **状态机语义重定义**：0=待支付(锁库存) / 1=已支付(留给 Mock 支付) / 2=库存不足 / 3=已关闭。消费者只锁库存置0，超时关闭置3，支付置1。
- **验证（走法B，用户一次性授权阿瑞执行）**：临时 `TIMEOUT_MINUTES` 由 30 改 1 → 重启应用 → 登录→加购商品17→下单 order23 → 初始 status=0、库存100→99、Redis=SUCCESS ✅ → 等 70 秒 → 再查 status=3(CLOSED)、库存回升100、Redis=CLOSED ✅（启动类 `@EnableScheduling` 确认在）。验证通过。**注意查询坑**：用变量传 token 会丢失导致 401 误判"没关单"，须用字面重新登录取 token 查才准。
- 验证后恢复：删 order23+明细、商品17库存回100、删 Redis 键，核对四项全绿。已把 `TIMEOUT_MINUTES` 改回 30（用户授权"验证完改回30"）。
- **待用户操作**：改回 30 后需重启应用才生效；超时关闭 4 文件（task/启动类/消费者/枚举）尚未 git 提交。
- 下一业务：Mock 支付（POST /order/pay/{id}，status 0→1），按用户要求"每次只给一个业务"单独推进。
